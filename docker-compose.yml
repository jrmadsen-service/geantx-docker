version: "3.3"

# Building:
#
# $ docker-compose up --no-start --build
#

services:
    #--------------------------------------------------------------------------#
    # Geant4 container
    geant4:
      image: geantx/geant4
      stdin_open: true
      tty: true
      build:
        context: .
        dockerfile: Dockerfile
        args:
          COMPILER_TYPE: "gcc"
          COMPILER_VERSION: "7"
          BUILD_TYPE: "Release"
          TIMEMORY: "OFF"
          SOFTWARE: "geant4"
